Contents of repository:
- nixos: Configuration files using flakes, different host configurations are seperated within host/ and needed commands are listed in README
- nvim: neovim configuration, simply the directory where it's needed
- shellconfigs: (depricated) configures isolated shell environements for different development related purposes
